/**
 * Constants for DocumentsHomeHub
 * 
 * Shared constants and CSS class definitions
 */

// Section header styling
export const sectionHeader = "text-base font-semibold text-gray-500";

// Tip badge styling
export const tipBadge =
  "inline-flex items-center gap-1 px-1.5 py-0 rounded-full border text-[10px] bg-amber-50 text-amber-700 border-amber-200";

// Card styling constants
export const cardBaseClasses =
  "group relative bg-[var(--bg-primary)] border border-[var(--border-color)] rounded-xl hover:border-[var(--accent-primary)] transition-all duration-200";

export const cardHoverClasses = "hover:shadow-md hover:scale-[1.01]";

// Row styling constants
export const rowBaseClasses =
  "group relative flex items-center gap-3 px-3 py-2 bg-[var(--bg-primary)] border-b border-[var(--border-color)] hover:bg-[var(--bg-hover)] transition-colors";

// Status stripe styling
export const statusStripeClasses =
  "absolute left-0 top-0 bottom-0 w-1 cursor-pointer hover:w-1.5 transition-all";

// Kanban card styling
export const kanbanCardClasses =
  "bg-white border border-gray-200 rounded-xl p-3 shadow-sm hover:shadow-md transition-shadow cursor-pointer";

// Priority badge styling
export const priorityBadgeClasses =
  "inline-flex items-center gap-1 px-2 py-0.5 rounded-full border text-xs font-medium";

// Date badge styling
export const dateBadgeClasses =
  "inline-flex items-center gap-1 px-2 py-0.5 rounded-md text-xs text-[var(--text-secondary)]";

// Tag pill styling
export const tagPillClasses =
  "inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs bg-[var(--accent-secondary)] text-[var(--accent-primary)] border border-[var(--accent-primary)]";

