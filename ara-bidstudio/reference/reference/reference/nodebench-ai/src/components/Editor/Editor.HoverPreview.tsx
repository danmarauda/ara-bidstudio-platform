import React from "react";

// Thin wrapper if we want a unified hover preview API later.
export const EditorHoverPreview: React.FC<{ children?: React.ReactNode }> = ({ children }) => {
  return <>{children}</>;
};

